class Order {
    int orderId;
    double baseAmount;
    static double deliveryCharge = 40; [cite: 25, 31]

    Order(int orderId, double baseAmount) { [cite: 26]
        this.orderId = orderId;
        this.baseAmount = baseAmount;
    }

    double calculateBill() { [cite: 27]
        return baseAmount + deliveryCharge;
    }
}

class RegularOrder extends Order { [cite: 29]
    RegularOrder(int id, double amt) { super(id, amt); }
    // No discount [cite: 29]
}

class PremiumOrder extends Order { [cite: 30]
    PremiumOrder(int id, double amt) { super(id, amt); }

    @Override
    double calculateBill() { [cite: 32]
        double discountedAmount = baseAmount * 0.80; // 20% discount [cite: 30]
        return discountedAmount + deliveryCharge;
    }
}