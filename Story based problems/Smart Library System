class Book {
    int bookId;
    String title;
    double price;
    static String libraryName = "City Central Library"; [cite: 7, 17]

    Book(int bookId, String title, double price) { [cite: 8]
        this.bookId = bookId;
        this.title = title;
        this.price = price;
    }

    double calculateFine(int daysLate) { [cite: 9]
        return 0;
    }
}

class TextBook extends Book { [cite: 12]
    TextBook(int id, String t, double p) { super(id, t, p); }

    @Override
    double calculateFine(int daysLate) { [cite: 14]
        return daysLate * 2; // 2 per day [cite: 15]
    }
}

class Magazine extends Book { [cite: 13]
    Magazine(int id, String t, double p) { super(id, t, p); }

    @Override
    double calculateFine(int daysLate) { [cite: 14]
        return daysLate * 5; // 5 per day [cite: 16]
    }
}