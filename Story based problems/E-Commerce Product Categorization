class Product {
    int productId;
    String productName; [cite: 67]

    Product(int id, String name) {
        this.productId = id;
        this.productName = name;
    }

    boolean isMatch(String keyword) { [cite: 68]
        return productName.contains(keyword); [cite: 69, 74]
    }
}

class ElectronicProduct extends Product { [cite: 70]
    String brand; [cite: 71]

    ElectronicProduct(int id, String name, String brand) {
        super(id, name);
        this.brand = brand;
    }

    @Override
    boolean isMatch(String keyword) { [cite: 72]
        return productName.equalsIgnoreCase(keyword) || brand.equalsIgnoreCase(keyword); [cite: 75]
    }
}